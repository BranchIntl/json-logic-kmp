# Linking the Compose and skiko graph into a single Wasm binary exhausts the Kotlin daemon's
# inherited default heap, intermittently, so it is raised rather than left to chance.
kotlin.daemon.jvmargs=-Xmx4g
